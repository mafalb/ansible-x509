---
- name: csr for {{ x509_alias }} is absent
  with_items: x509_src.certs
  file:
    path: "{{ x509.catrustdir }}/{{ x509_alias }}.pem"
    state: absent
  notify:
  - build ca trust store
  - enable ca trust store
...
