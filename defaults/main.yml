---

x509:
  supported_os_families:
    - RedHat
  alias: "{{ x509_alias }}"
  days: 825
  group: root
  state: "{{ x509_state|default('present') }}"
  local_cert_store: "{{ playbook_dir + '/files/x509/' }}"
